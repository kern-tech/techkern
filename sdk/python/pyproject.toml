[project]
name = "techkern-sdk"
version = "0.4.0"
description = "Python client for the techkern MCP server — live Solana context for AI agents"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]
license = { text = "MIT" }
keywords = ["mcp", "solana", "ai-agents", "techkern"]
